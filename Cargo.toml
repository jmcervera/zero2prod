[package]
name = "zero2prod"
version = "0.1.0"
edition = "2021"

[lib]
# name = "zero2prod" # this is the default if omitted
path = "src/lib.rs"

[[bin]]
name = "zero2prod"
path = "src/main.rs"

[dependencies]
actix-web = "4"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
